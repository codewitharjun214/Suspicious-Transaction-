{
  "name": "project-root",
  "private": true,
  "scripts": {
    "backend": "npx kill-port 4000 && npm --prefix node-backend start",
    "frontend": "npm --prefix react-frontend run dev",
    "dev": "npx kill-port 4000 && npx concurrently \"npm --prefix node-backend start\" \"npm --prefix react-frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "kill-port": "^1.6.0"
  }
}
